/**
 * Canonical public origin for metadata-only URLs.
 *
 * This is intentionally checked-in public configuration, not product runtime configuration. The
 * production origin is recorded in README.md, deploy/README.md, and the dated deployment status.
 */
export const publicOrigin = 'https://gol.network' as const;
